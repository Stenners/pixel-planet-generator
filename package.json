{
  "name": "pixel-planet-gen",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel ./index.html",
    "build": "parcel build ./index.html",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "parcel": "^1.12.3"
  }
}
